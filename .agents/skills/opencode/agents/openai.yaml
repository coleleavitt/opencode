interface:
  display_name: "Opencode"
  short_description: "Repo-specific patterns and workflows for opencode"
  default_prompt: "Use the opencode repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true